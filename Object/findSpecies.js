// Create a function called findSpecies that takes in an array, loops through
// the array and returns an array of all of the species values for each object.

var findSpecies;
function findSpecies(animal){
var animal={
    Species :"rabbit",
    name :"samir",
    age :2,
    weight :20,
    assingns :"jump",
}
}
